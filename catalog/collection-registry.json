{
  "schemaVersion": 1,
  "note": "OUR enable switch (SPEC-collection-registry.md; this file is the SWITCH (collection-registry.json) - a collection's own config is the separate collection.json in nft-collections/<slug>/). Lives in catalog/ beside trusted/current.json - catalog is the home for registries of known entities. Collection data lives in thealliancedao/nft-collections/<slug>/; DAO data in dao-originations/<slug>/. Enabling an entry tells the crons to capture that folder and the site to light the selector + shift the dynamic pages. aDAO's hard-coded paths stay authoritative until the registry-driven path verifies (parallel-run law).",
  "data_repo": "thealliancedao/nft-collections",
  "dao_repo": "thealliancedao/dao-originations",
  "collections": [
    {
      "slug": "adao",
      "enabled": true,
      "config": "adao/collection.json",
      "status": "seed - documents current hard-coded values; not yet driving crons or site"
    }
  ]
}