{
  "_meta": {
    "purpose": "Weights for the composite token grade. Editable here (and in the override tool); recorded in cron output so every scored snapshot says which weights produced it.",
    "formula": "overall = price * price_confidence + identity * identity_confidence",
    "note": "Adjust the numbers, re-run the cron, grades shift — no code change. Weights should sum to 1.0."
  },
  "price": 0.75,
  "identity": 0.25
}
