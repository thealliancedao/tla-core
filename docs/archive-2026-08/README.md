# archive-2026-08 — INERT REFERENCE. Nothing reads this folder.

Ten documents rescued from `defipatriot/website-adao-core` on 2026-08-11,
immediately before the personal repos were deleted. They are **intent and
design history**, not data: mission, direction, project knowledge, session
closeout, and three specs that exist nowhere else (ai-assistant,
nft-onboarding-blueprint, portfolio-tracker).

**Deliberately placed in an archive folder no cron or page points at**, so
there is zero risk of stale content re-entering the live system. If something
here becomes relevant again, it gets rewritten into
`docs/pending-changes/` as a current spec — never read from here.

NOT archived (checked, org is ahead): the six changelogs in that repo were
stale relative to `docs/changelogs/` (e.g. index-log Rev 3.55 vs org
2026-08-09; tla-log Rev 5.5 vs org T3.4).
